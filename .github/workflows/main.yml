name: UISharp Pipeline

on:
  push:
    branches: [ "main" , "feature/9-automatic-versioning" ]
  pull_request:
    branches: [ "*" ]
  workflow_dispatch:
    inputs:
      workflow_type:
        description: "Select workflow type"
        required: true
        default: "default"
        type: choice
        options:
          - release
          - default

jobs:
  versioning:
    runs-on: ubuntu-latest
    outputs:
      ui_version: ${{ steps.get_version.outputs.ui_version }}
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Ensure full history for GitVersion

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Install GitVersion
      run: dotnet tool install --global GitVersion.Tool

    - name: Get version from UISharp
      id: get_version
      run: |
        VERSION=$(dotnet gitversion /output json | jq -r '.SemVer')  # Extract version
        echo "ui_version=$VERSION" >> $GITHUB_ENV
        echo "ui_version=$VERSION" >> $GITHUB_OUTPUT

    - name: Create and Push Git Tag for UISharp
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/feature/9-automatic-versioning'
      run: |
        VERSION=${{ steps.get_version.outputs.ui_version }}
        git tag "v$VERSION"
        git push origin "v$VERSION"

  build:
    needs: versioning  # Ensures versioning completes first
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            rid: linux-x64
          - os: macos-latest
            rid: osx-x64
          - os: windows-latest
            rid: win-x64

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Ensure full history for GitVersion

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore --configuration Release

    - name: Publish
      run: dotnet publish -c Release -r ${{ matrix.rid }} --self-contained -o publish/${{ matrix.rid }}

    - name: Upload Artifact
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event.inputs.workflow_type == 'release'
      uses: actions/upload-artifact@v4
      with:
        name: UISharp-${{ matrix.rid }}
        path: publish/${{ matrix.rid }}/*
